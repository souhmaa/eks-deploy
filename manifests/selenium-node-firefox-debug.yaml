apiVersion: apps/v1
kind: Deployment
metadata:
  name: selenium-node-firefox-debug
  labels:
    app: selenium-node-firefox-debug
spec:
  replicas: 2
  selector:
    matchLabels:
      app: selenium-node-firefox-debug
  template:
    metadata:
      labels:
        app: selenium-node-firefox-debug
    spec:
      volumes:
        - name: dshm
          emptyDir:
            medium: Memory
      containers:
        - name: selenium-node-firefox-debug
          image: selenium/node-firefox-debug:3.141
          ports:
            - containerPort: 5900
          volumeMounts:
            - mountPath: /dev/shm
              name: dshm
          env:
            - name: HUB_HOST
              value: "selenium-hub"
            - name: HUB_PORT
              value: "4444"
            - name: NODE_MAX_INSTANCES
              value: "2"
            - name: NODE_MAX_SESSION
              value: "2"
          resources:
            limits:
              memory: "1000Mi"
              cpu: ".5"
